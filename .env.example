# Meta Ads Intelligence runtime configuration
# NEVER commit a real access token.

# Runtime access token supplied by a secure secret manager.
META_ACCESS_TOKEN=

# Optional override. Leave empty to use the default Graph API host.
META_GRAPH_API_BASE_URL=https://graph.facebook.com
